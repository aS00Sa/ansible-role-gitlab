---
- name: Ensure self-service helper directories exist
  ansible.builtin.file:
    path: "{{ item }}"
    state: directory
    owner: root
    group: root
    mode: "0755"
  loop:
    - /usr/local/sbin
    - /var/log/gitlab
    - /var/lock

- name: Install self-service diagnostics script
  ansible.builtin.template:
    src: self-service-diagnostics.sh.j2
    dest: /usr/local/sbin/gitlab-self-service-diagnostics.sh
    owner: root
    group: root
    mode: "0750"

- name: Configure self-service diagnostics cron
  ansible.builtin.cron:
    name: gitlab-self-service-diagnostics
    user: "{{ gitlab_self_service_diagnostics_cron_user }}"
    minute: "{{ gitlab_self_service_diagnostics_cron_minute }}"
    hour: "{{ gitlab_self_service_diagnostics_cron_hour }}"
    day: "{{ gitlab_self_service_diagnostics_cron_day }}"
    month: "{{ gitlab_self_service_diagnostics_cron_month }}"
    weekday: "{{ gitlab_self_service_diagnostics_cron_weekday }}"
    job: "/usr/local/sbin/gitlab-self-service-diagnostics.sh"
